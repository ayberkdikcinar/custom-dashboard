enum ChartType {
  PIE = 'pie-chart',
  LINE = 'line-chart',
  AREA = 'stacked-area-chart',
  BAR = 'bar-chart',
  SCATTER = 'scatter-chart',
  GAUGE = 'gauge-chart',
  COLUMN = 'column-chart',
  WATERFALL = 'waterfall-chart',
}

export { ChartType };
